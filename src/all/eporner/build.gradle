ext {
    extName = 'Eporner'
    extClass = '.Eporner'
    extVersionCode = 2  // Increment with each update
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:playlist-utils'))
}
