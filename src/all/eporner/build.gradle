ext {
    extName = 'Eporner'
    extClass = '.Eporner'
    extVersionCode = 2  // Keep this incremented
    isNsfw = true
}

// This already includes ALL essential dependencies
apply from: "$rootDir/common.gradle"

// Empty dependencies block is PERFECTLY FINE
dependencies {
    // Nothing needed here
}
