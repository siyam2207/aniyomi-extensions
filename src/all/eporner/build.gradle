ext {
    extName = 'Eporner'
    extClass = '.Eporner'
    extVersionCode = 2  // Incremented version
    isNsfw = true
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:playlist-utils'))
    implementation(project(':lib:unpacker'))  // Add for JavaScript unpacking
}
